// GitHub Progress configuration. Nothing UI-related lives here — only what
// repos to feature. Edit `featuredRepositories` any time; no component
// changes needed.

export const GITHUB_USERNAME = 'ArjithSiva'

// Your 3 best/favourite repos, by exact repo name (the part after
// github.com/ArjithSiva/ in the URL) — not the display title. Order here is
// the display order. If a name doesn't match a public repo (typo, or the
// repo is private) it's silently skipped rather than shown broken, so it's
// safe to edit without testing every URL.
export const featuredRepositories: string[] = [
  // TODO: replace with your actual repo names, e.g. 'puthurkattu-cims'
  'repo-name-1',
  'repo-name-2',
  'repo-name-3',
]
